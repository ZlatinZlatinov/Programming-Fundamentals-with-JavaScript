function multiplicationTable(num) { 
    let sum = 0;
     for (let x = 1; x <= 10; x++){ 
         sum = num*x;
         console.log(`${num} X ${x} = ${sum}`);
     }
} 
multiplicationTable(5); 
// 100/100 ezzz